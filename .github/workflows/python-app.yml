name: Python application (disabled)

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  no-op:
    runs-on: ubuntu-latest
    steps:
    - run: echo "CI desabilitado a pedido."
